# Anthropic API key — get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-api03-...

# JWT signing secret — replace with a long random string in production
JWT_SECRET=replace-with-a-long-random-secret-key-minimum-32-chars

# Hardcoded credentials for the MVP (change for production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# Your local SP-API MCP server endpoint
MCP_SERVER_URL=http://localhost:3000/sse
